# Runtime dependencies for the OPTIONAL dense sentence-embedding signal
# (src/embed.py: the S5 dense retrieval route and the S6 rerank cosine term).
# All three ship as prebuilt wheels - no build toolchain, no network at runtime.
#
# The agent runs WITHOUT these. If the imports fail or the prebuilt artifact
# under data/embeddings/ is missing, the dense signal self-disables and the
# pipeline is the pure-stdlib BM25 one (see README "Disclosure", and
# tests/test_regression.py::test_degrade_path_holds_the_floor).
numpy>=1.24
onnxruntime>=1.16
tokenizers>=0.15

# Build-time only (tools/build_embeddings.py, run once on a dev machine to
# produce data/embeddings/*). NOT needed for scoring:
#   pip install huggingface_hub
#
# Offline experiment tooling only (tools/fit_weights_pairwise.py, the
# weight-learning fitter described in docs/team/pairwise.md). NOT needed for
# scoring, and not imported by anything on the agent's path:
#   pip install numpy scikit-learn
